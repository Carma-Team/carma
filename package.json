{
  "name": "carma-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mobile",
    "mock-server/local-server"
  ],
  "scripts": {
    "mobile:start":  "npm run start  --workspace=mobile",
    "mobile:test":   "npm run test   --workspace=mobile",
    "mobile:lint":   "npm run lint   --workspace=mobile",
    "mock:dev":      "npm run dev    --workspace=mock-server/local-server"
  },
  "engines": {
    "node": ">=20"
  }
}
